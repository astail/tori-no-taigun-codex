{
  "name": "tori-no-taigun-codex",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ブラウザで遊べるオリジナル鳥リズムゲーム",
  "scripts": {
    "audio": "node scripts/generate-audio.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs",
    "verify:build": "node scripts/verify-build.mjs",
    "test": "node --test",
    "check": "npm run test && npm run build && npm run verify:build"
  },
  "engines": {
    "node": ">=22"
  }
}
